server:
  maxWorkers: 10
  port: 50058

client:
  gateway:
    host: "localhost"
    port: 50051

  storageNodes:
    - host: "localhost"
      port: 50059
      alias: "storage-01"

store:
  path: "storage.db"
