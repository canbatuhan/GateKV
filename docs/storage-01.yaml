server:
  maxWorkers: 10
  port: 50059

client:
  gateway:
    host: "localhost"
    port: 50051

  storageNodes:
    - host: "localhost"
      port: 50060
      alias: "storage-01"

store:
  path: "storage.db"
