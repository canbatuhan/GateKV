syntax = "proto3";

package gatekv_gateway;

service GateKV_Gateway {
    // Services
    rpc Sample(Message) returns (ACK) {}
}

message Message {
    string payload = 1;
}

message ACK {
    uint32 counter = 1;
}